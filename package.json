{
  "name": "subgraphito",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ gabrielcamba/subgraphito",
    "create-local": "graph create --node http://localhost:8020/ gabrielcamba/subgraphito",
    "remove-local": "graph remove --node http://localhost:8020/ gabrielcamba/subgraphito",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gabrielcamba/subgraphito"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
